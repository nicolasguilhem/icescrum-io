import { ConfigPropertyDirective } from './config-property.directive';

describe('ConfigPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
